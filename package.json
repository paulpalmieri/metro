{
  "name": "paris-metro",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "vercel dev",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "snapshot:siri": "node scripts/capture-siri-snapshot.mjs",
    "snapshot:calibrate": "node scripts/capture-siri-snapshot.mjs --mode=metro --count=120 --interval=60",
    "analyze:calibration": "node scripts/analyze-siri-calibration.mjs --output=../src/data/movement-calibration.json --movement=stable"
  },
  "dependencies": {
    "@fontsource/fira-sans-condensed": "^5.3.0",
    "@vercel/analytics": "^2.0.1"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
